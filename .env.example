# Frontend Environment Variables (.env.production)
# Copy this to .env.production for Vercel deployment

# API URL - Connect to your existing backend
VITE_API_URL=https://quluub-reborn-project-33-8lca.onrender.com/api

# Alternative: Use relative URL if deploying full-stack to same domain
# VITE_API_URL=/api

# Note: Backend environment variables should be set on your Render deployment
# including MONGODB_URI, JWT_SECRET, payment keys, etc.
